import React from 'react';

const Product = ({product}) => {
    const {_id, picture, price, name, quantity, description, supplierName,} = product;
    return (
        <div>
            
        </div>
    );
};

export default Product;